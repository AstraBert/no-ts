# No

The 'no' command is the same as the 'yes' command, but it prints 'n'.

## Installation

With npm:

```bash
npm install -g @cle-does-things/no
```

With brew:

```bash
brew tap AstraBert/no-ts
brew install cle-does-things-no
```
